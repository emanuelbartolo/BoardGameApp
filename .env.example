# Copy this file to `.env` and fill in the values. Do NOT commit your real .env file.

# OpenRouter API key (secret) used by the Cloud Function
OPENROUTER_API_KEY=

# Optional: override default LLM model used by the Cloud Function
# Example: google/gemma-3-27b-it:free
OPENROUTER_MODEL=google/gemma-3-27b-it:free

# (Optional) Firebase project id if you run scripts that require it
# FIREBASE_PROJECT=your-firebase-project-id
